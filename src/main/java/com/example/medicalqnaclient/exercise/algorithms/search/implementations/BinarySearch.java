package com.example.medicalqnaclient.exercise.algorithms.search.implementations;

import com.example.medicalqnaclient.exercise.algorithms.search.interfaces.Search;

public class BinarySearch<E> implements Search<E> {
    @Override
    public int search(E[] array, E key) {
        return 0;
    }
}
